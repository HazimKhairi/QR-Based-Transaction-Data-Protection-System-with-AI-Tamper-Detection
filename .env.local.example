# Copy this file to .env.local at the project root:
#
#   cp .env.local.example .env.local
#
# The default points the frontend at the Flask backend running on
# localhost:5000. Change it only if you run the backend on a different
# host or port.

NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
